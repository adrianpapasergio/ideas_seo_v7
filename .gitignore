# Entorno / secretos
venv/
.env
__pycache__/
*.pyc
*.log

# Bases locales (por defecto ignoradas)
*.db
*.sqlite

# Ignorar archivos de configuración de macOS
.DS_Store

# Ignorar freeze release y otros scripts de inicio
freeze_release.sh
project_start.sh
project_start.ps1


# JSONs ignorados por defecto...
*.json

# ...pero versionamos los que nos interesan
!data/ideas/*.json
!data/counters.json

# Dejar de trackear TODOS los JSON bajo data/ideas (pero conservarlos en tu disco)
git rm --cached -r data/ideas/*.json

# Commit de esta limpieza
git commit -m "chore: stop tracking per-user idea JSONs (local data)"

# Subir
git push origin main

# Estáticos y templates SIEMPRE versionados
# (no los ignores)
# No versionar JSON de usuarios
data/ideas/*.json
.DS_Store
**/.DS_Store
